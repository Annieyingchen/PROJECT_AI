# 1 分钟演示视频脚本（M2 · 录视频素材）

> 用途：求职作品集里的项目演示短视频。面试官 / HR 快速看懂项目在做什么。
> 时长：60 秒 ｜ 工具：QuickTime 录屏 + 可选加字幕

---

## 分镜脚本

| 时间 | 画面 | 旁白（台词） | 操作 |
|---|---|---|---|
| 0:00–0:05 | 打开 GitHub 仓库页 `Annieyingchen/PROJECT_AI` | 「这是一个从 0 手写的 AI 问答 Agent，只有 600 行 Python、一个第三方依赖」 | 光标指向 Star / 代码文件列表 |
| 0:05–0:15 | 滚动 README 架构图 | 「分层设计：配置 → LLM 调用 → 上下文记忆 → 问答编排 → 工具调用，没有用 LangChain」 | 滚到架构图那段停 2 秒 |
| 0:15–0:25 | 终端 `python app.py` → 浏览器打开 localhost:7860 | 「一句话启动，浏览器里直接体验」 | 输入启动命令，切到浏览器 |
| 0:25–0:40 | 输入「现在几点」 | 「注意看——模型不是心算，而是真的调用了工具拿真实时间」 | 打字 + 等回复 |
| 0:40–0:52 | 输入「算一下 1024×768」 | 「再来一个计算，验证工具调用循环」 | 打字 + 等回复 |
| 0:52–1:00 | 回到仓库页，指 README 扩展路线图 | 「后续方向：RAG 检索增强、多模型路由。代码全部开源，欢迎交流」 | 滚到扩展路线图，结束 |

---

## 录屏操作清单（录之前准备好）

1. **预热**：先手动跑一遍 `python app.py`，确认模型能正常返回（避免录的时候翻车）。
2. **窗口就绪**：开好两个窗口——① 终端（已 cd 到项目目录）② 浏览器（停在 localhost:7860）。
3. **清空历史**：点一次「清空上下文」，保证演示从干净状态开始。
4. **关闭通知**：录屏前把系统通知/微信弹窗关掉，避免打断。

## 录制命令（macOS）

```bash
# 方式一：QuickTime Player → 文件 → 新建屏幕录制（自带，最简单）
# 方式二：命令行 ffmpeg（如果装了）
ffmpeg -f avfoundation -i "1:none" -r 30 -t 60 demo.mp4
```

## 可选：加字幕（提升 HR 体验）

- 用「剪映 / CapCut」自动生成字幕，再把旁白文字贴进去校对一遍。
- 字幕节奏对上面的分镜表即可。

---

## 演示问题清单（3 个核心问题）

| # | 问题 | 验证点 |
|---|---|---|
| 1 | 现在几点 | 工具调用：`get_current_time` 真实返回 |
| 2 | 算一下 1024×768 | 工具调用：`calculator` 精确计算 |
| 3 | 北京今天天气怎么样？ | 纯问答（模型知识回答）+ 可接多轮追问 |

---

## 视频命名建议

```
PROJECT_AI-demo-1min.mp4
```

上传到 GitHub Release / 仓库根目录 / 或投递简历时作为附件链接。
